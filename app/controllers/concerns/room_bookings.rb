# TEST CURRENTLY NOT IN USE

# module RoomBookings
#   extend ActiveSupport::Concern


#   def show

#     def room_bookings(room, user)
#       Booking.where(motel_id: user.motel_id).select { |booking| booking.room_id == room.id }
#     end
#     room_bookings(room, current_user)
#     @room_bookings = room_bookings(room, current_user)
#   end



#   private

#   # def room_bookings(room, user)
#   #   Booking.where(motel_id: user.motel_id).select { |booking| booking.room_id == room.id }
#   # end

# end
